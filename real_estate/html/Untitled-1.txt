#include <stdio.h>
#include <conio.h>

int a,b,c;
int array[20];

clrscr();

for(int i=0; i<array.length; i++){
    scanf("%d", array[i]);
}

for(int j=0; j<array.length: j++){
    printf("%d", &array[i]);
}

getch();
}
}